<%= simple_form_for @card do |form| %>
  <%= yield %>

  <%= form.input :title %>
  <%= form.input :description %>
  <%= form.input :column_id, 
    collection: Card::STATUSES.map { |s| [s.humanize, s] },
    prompt: "Select status" %>
  <%= form.button :submit, "Save Card" %>
  
<% end %>
``