<div class="card" data-card-id="<%= card.id %>">
  <%= link_to project_board_card_path(@project, @board, card), class: 'card__title', data: { turbo: false } do %>
    <%= card.title %>
  <% end %>
  <div class="card__description">
    <%= card.description %>
  </div>
</div>



